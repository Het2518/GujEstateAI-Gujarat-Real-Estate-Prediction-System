"""features.py

Feature engineering utilities.
"""
import pandas as pd

def build_features(df: pd.DataFrame) -> pd.DataFrame:
    """Return a dataframe with engineered features."""
    # placeholder: implement feature transformations
    return df.copy()
